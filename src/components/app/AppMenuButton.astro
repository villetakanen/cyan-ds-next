<cn-app-menu-button
  class="hide-on-desktop appMenuButton"
></cn-app-menu-button>

<style>
  .appMenuButton {
    position: fixed;
    top: var(--cn-grid);
    left: calc(var(--cn-grid) * 1.5);
  }
</style>
