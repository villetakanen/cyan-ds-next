<cn-app-menu-button
  class="hide-on-desktop appMenuButton"
></cn-app-menu-button>

<style>
  .appMenuButton {
    position: fixed;
    top: var(--cn-grid-size);
    left: calc(var(--cn-grid-size) * 1.5);
  }
</style>
